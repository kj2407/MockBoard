# Backend

Express API for MockBoard (`server.js`, `routes/`, `utils/`). This project uses a single Git repository at the repo root—do not run `git init` inside `backend/`. If you did, delete the `backend/.git` file or folder so the parent repo tracks these files.
